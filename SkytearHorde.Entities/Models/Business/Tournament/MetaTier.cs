namespace SkytearHorde.Entities.Models.Business.Tournament
{
    public enum MetaTier
    {
        S = 0,
        A = 1,
        B = 2,
        C = 3,
        D = 4,
        /// <summary>Below the sample gate. Shown, but explicitly not ranked — the honesty signal.</summary>
        Unranked = 5
    }
}
