namespace SkytearHorde.Entities.Constants
{
    public static class GroupConstants
    {
        // Id for decks that only have a main group (like card games)
        public const int MainGroupId = -1;
    }
}
